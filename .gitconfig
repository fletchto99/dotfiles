[core]
  excludesfile = ~/.gitignore_global
  # FSEvents-based file system monitor — speeds up `git status` 5-10x on large repos.
  # Requires git 2.37+. Free on macOS; ignored elsewhere if unsupported.
  fsmonitor = true
  untrackedCache = true
  # Editor: VS Code Insiders. Works on every platform that has the Insiders build
  # installed (macOS / Linux / Windows / WSL). The one realistic gap is plain
  # GitHub Codespaces, where only `code` is in PATH by default; override locally
  # there with `git config --global core.editor "code --wait"` if needed.
  editor = code-insiders --wait

[user]
  name = Matt Langlois
  email = fletchto99@gmail.com

[init]
  defaultBranch = main

[commit]
  gpgsign = true
  # Show the diff in the commit-message editor so you can sanity-check
  # what you're committing while writing the message.
  verbose = true

[tag]
  # If commits sign, tags should too — they're what releases pin to.
  gpgsign = true
  # Sort tags by version, not alphabetically — 1.10 comes after 1.9, not
  # after 1.1. Huge readability win for any repo using semver tags.
  sort = version:refname

[github]
  user = fletchto99
  shelluser = matt

[protocol]
  version = 2

[fetch]
  prune = true
  # Maintain a commit-graph file that speeds up log/blame/walks on large repos.
  writeCommitGraph = true
  # Fetch from multiple remotes in parallel (0 = git picks based on CPU count).
  # Noticeable in repos with origin + upstream + fork.
  parallel = 0
  # Skip "(forced update)" detection — speeds up fetches in repos with many
  # refs at the cost of losing that line from fetch output.
  showForcedUpdates = false

[push]
  default = simple
  autoSetupRemote = true
  # Push annotated tags reachable from pushed commits without needing --tags.
  followTags = true

[pull]
  rebase = true

[rebase]
  # `fixup!` / `squash!` commits get auto-arranged on `rebase -i`. Pairs with
  # `git commit --fixup=<sha>` (see the `fixup` alias below) to make pre-PR
  # cleanup trivial.
  autoSquash = true
  # Auto-stash dirty worktree before rebase, pop after. Removes the "you have
  # unstaged changes, please commit or stash" friction without losing work.
  autoStash = true
  # When rebasing a stack of branches, intermediate branches that point at
  # rebased commits move forward with them. Essential for stacked-PR workflows.
  # Requires git 2.38+.
  updateRefs = true
  # Warn if `rebase -i` is about to drop commits (e.g. accidentally deleted a
  # `pick` line). `error` would abort entirely, which is too strict.
  missingCommitsCheck = warn

[rerere]
  # Reuse recorded resolution — once you resolve a conflict, git remembers it
  # and auto-applies the same resolution next time. Big rebase time-saver.
  enabled = true
  # Also stage the replayed resolution automatically, so the conflict literally
  # vanishes mid-rebase without a manual `git add`.
  autoUpdate = true

[branch]
  # `git branch` lists most-recently-committed branches first, not alphabetical.
  sort = -committerdate

[column]
  # `git branch`, `git tag`, `git stash list` print in columns like ls(1) when
  # the terminal is wide enough. Big readability upgrade on a wide terminal.
  ui = auto

[help]
  # When you typo `git puhs`, git asks "did you mean push?" instead of either
  # running puhs silently or auto-correcting after a tiny delay.
  autocorrect = prompt

[log]
  # ISO dates everywhere — sortable, copy-pasteable, no "Mon Jun 4" prose.
  date = iso

[grep]
  # `git grep` prints line numbers by default. Annoying that this is opt-in.
  lineNumber = true

[color]
  ui = true

[color "branch"]
  current = magenta
  remote = cyan

[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red reverse
  new = green reverse
  whitespace = white reverse

[color "status"]
  added = green
  changed = cyan
  untracked = blue bold
  branch = magenta

[diff]
  renames = copies
  # Histogram produces more readable diffs than the default `myers`, especially
  # for code with lots of structural changes.
  algorithm = histogram
  # Color lines that moved (vs. added/deleted) distinctly — huge help when
  # reviewing refactors.
  colorMoved = zebra
  # Treat indentation-only differences as moves rather than separate add/delete.
  colorMovedWS = allow-indentation-change
  # Replace generic a/ b/ in diff headers with mnemonic prefixes:
  # i/ (index)  w/ (worktree)  c/ (commit)  o/ (object).
  # Makes `git diff`, `git diff --cached`, `git diff HEAD` self-labeling.
  mnemonicPrefix = true
  tool = vscode

[difftool]
  prompt = false

[difftool "vscode"]
  cmd = code-insiders --wait --diff $LOCAL $REMOTE

[mergetool]
  prompt = false

[mergetool "vscode"]
  # VS Code's native 3-way merge UI (--merge) rather than --diff, which would
  # only show LOCAL vs REMOTE without letting you edit MERGED.
  cmd = code-insiders --wait --merge $REMOTE $LOCAL $BASE $MERGED

[merge]
  # zdiff3 shows the common-ancestor version between ||||||| markers alongside
  # both sides — makes conflict resolution noticeably faster than the default
  # `merge` style. Requires git 2.35+.
  conflictStyle = zdiff3
  tool = vscode

[alias]
  # Lol for when I forget
  aliases = config --get-regexp alias

  # List remotes
  rv = remote -v

  # Patch mode for adding so I don't add random stuff
  addp = add --patch

  # Shortcut to diff tool
  dt = difftool

  # Shortcut to diff tool on staged files
  dts = difftool --staged

  # Show the diff between the latest commit and the current state
  dl = !"git diff-index --quiet HEAD -- || clear; git --no-pager diff --patch-with-stat"

  # `git di $number` shows the diff between the state `$number` revisions ago and the current state
  di = !"d() { git diff --patch-with-stat HEAD~$1; }; git diff-index --quiet HEAD -- || clear; d"

  # Find branches containing commit - lets track down what broke things!!
  fb = "!f() { git branch -a --contains $1; }; f"

  # Recently committed to branches
  recent = for-each-ref --count=30 --sort=-committerdate --format="%(committerdate:relative)%09%(refname:short)" refs/heads

  # Pretty logs
  lg = !"git lg1"
  lg1 = !"git lg1-specific --all"
  lg2 = !"git lg2-specific --all"
  lg3 = !"git lg3-specific --all"

  # Pretty log commands
  lg1-specific = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)'
  lg2-specific = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(auto)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)'
  lg3-specific = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset) %C(bold cyan)(committed: %cD)%C(reset) %C(auto)%d%C(reset)%n''          %C(white)%s%C(reset)%n''          %C(dim white)- %an <%ae> %C(reset) %C(dim white)(committer: %cn <%ce>)%C(reset)'

  # `git fixup <sha>` → creates a fixup! commit targeting <sha>. Then
  # `git rebase -i <sha>^` auto-arranges it (rebase.autoSquash = true above).
  fixup = commit --fixup

  # Undo the last commit but keep all changes staged. The "oops, wrong
  # message / forgot a file" rescue.
  uncommit = reset --soft HEAD~1

  # Reads better than `git reset HEAD <file>`.
  unstage = reset HEAD --

# Machine-local include. Populated per-machine by `script/bootstrap`:
#
#   - macOS: symlinked to .gitconfig_macos (SSH-signing via 1Password's
#     op-ssh-sign, osxkeychain credential helper, and https→ssh URL rewriting
#     for github.com — all of which depend on Mac-only tooling).
#   - Linux / Codespaces: file is absent, and git silently no-ops on missing
#     include paths, so the same committed .gitconfig works everywhere without
#     needing OS detection (which git's includeIf doesn't natively support —
#     it only knows gitdir, onbranch, and hasconfig conditions, so the
#     previous `gitdir:/Users/` heuristic both over-matched on Linux boxes
#     with /Users/ paths and under-matched on macOS clones outside /Users/).
[include]
  path = ~/.gitconfig.local

# Both an https and an ssh variant are needed because `hasconfig:remote.*.url`
# matches against the URL as stored in .git/config (which is whatever you typed
# at clone time), not the rewritten form that insteadOf produces at fetch/push
# time. gitconfig has no boolean-OR syntax for conditional includes, so each
# stored URL form gets its own stanza pointing at the same file.
[includeIf "hasconfig:remote.*.url:https://github.com/github/**"]
  path = .gitconfig_work

[includeIf "hasconfig:remote.*.url:git@github.com:github/**"]
  path = .gitconfig_work
